--- a/purpur-server/build.gradle.kts
+++ b/purpur-server/build.gradle.kts
@@ -25,15 +25,15 @@
     gitFilePatches = false
 
     // Purpur start - Rebrand
-    val purpur = forks.register("purpur") {
+    val multipaper = forks.register("multipaper") {
         upstream.patchDir("paperServer") {
             upstreamPath = "paper-server"
             excludes = setOf("src/minecraft", "patches", "build.gradle.kts")
-            patchesDir = rootDirectory.dir("purpur-server/paper-patches")
+            patchesDir = rootDirectory.dir("multipaper-server/paper-patches")
             outputDir = rootDirectory.dir("paper-server")
         }
     }
-    activeFork = purpur
+    activeFork = multipaper
     // Purpur end - Rebrand
 
     /*updatingMinecraft { // Purpur start - TODO - snapshot - we don't need this
@@ -137,7 +137,7 @@
 }
 
 dependencies {
-    implementation(project(":purpur-api")) // Purpur
+    implementation(project(":multipaper-api")) // MultiPaper
     implementation("ca.spottedleaf:leafpile:1.0.0")
     implementation("org.jline:jline-terminal-ffm:3.27.1") // use ffm on java 22+
     implementation("org.jline:jline-terminal-jni:3.27.1") // fall back to jni on java 21
@@ -198,19 +198,19 @@
         val build = System.getenv("BUILD_NUMBER") ?: null
         val buildTime = if (build != null) Instant.now() else Instant.EPOCH // Purpur
         val gitHash = git.exec(providers, "rev-parse", "--short=7", "HEAD").get().trim()
-        val implementationVersion = "$mcVersion-${build ?: "DEV"}-$gitHash"
+        val implementationVersion = "MultiPaper-$mcVersion-${build ?: "DEV"}-$gitHash"
         val date = git.exec(providers, "show", "-s", "--format=%ci", gitHash).get().trim()
         val gitBranch = git.exec(providers, "rev-parse", "--abbrev-ref", "HEAD").get().trim()
         attributes(
             "Main-Class" to "org.bukkit.craftbukkit.Main",
-            "Implementation-Title" to "Purpur", // Purpur
+            "Implementation-Title" to "MultiPaper", // MultiPaper
             "Implementation-Version" to implementationVersion,
             "Implementation-Vendor" to date,
-            "Specification-Title" to "Purpur", // Purpur
+            "Specification-Title" to "MultiPaper", // MultiPaper
             "Specification-Version" to project.version,
-            "Specification-Vendor" to "Purpur Team", // Purpur
-            "Brand-Id" to "purpurmc:purpur", // Purpur
-            "Brand-Name" to "Purpur", // Purpur
+            "Specification-Vendor" to "MultiPaper Team", // MultiPaper
+            "Brand-Id" to "multipaper:multipaper", // MultiPaper
+            "Brand-Name" to "MultiPaper", // MultiPaper
             "Build-Number" to (build ?: ""),
             "Build-Time" to buildTime.toString(),
             "Git-Branch" to gitBranch,

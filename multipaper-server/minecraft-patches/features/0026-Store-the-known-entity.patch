From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: PureGero <puregero@gmail.com>
Date: Tue, 8 Mar 2022 21:43:27 +1000
Subject: [PATCH] Store the known entity


diff --git a/net/minecraft/world/level/entity/PersistentEntitySectionManager.java b/net/minecraft/world/level/entity/PersistentEntitySectionManager.java
index a77985b2dd7137d8eea03909403fc08e89376d73..12b4e41d6323f969e0731b83dfef92d71c0df3b1 100644
--- a/net/minecraft/world/level/entity/PersistentEntitySectionManager.java
+++ b/net/minecraft/world/level/entity/PersistentEntitySectionManager.java
@@ -15,9 +15,6 @@ import it.unimi.dsi.fastutil.longs.Long2ObjectMap.Entry;
 import java.io.IOException;
 import java.io.UncheckedIOException;
 import java.io.Writer;
-import java.util.List;
-import java.util.Queue;
-import java.util.Set;
-import java.util.UUID;
+import java.util.*;
 import java.util.function.Consumer;
 import java.util.stream.Collectors;
@@ -32,6 +32,6 @@ import net.minecraft.util.VisibleForDebug;
 public class PersistentEntitySectionManager<T extends EntityAccess> implements AutoCloseable {
     private static final Logger LOGGER = LogUtils.getLogger();
-    private final Set<UUID> knownUuids = Sets.newHashSet();
+    public final HashMap<UUID, T> knownUuids = new HashMap<>(); // MultiPaper - store the known entity
     private final LevelCallback<T> callbacks;
     public final EntityPersistentStorage<T> permanentStorage;
     private final EntityLookup<T> visibleEntityStorage;
@@ -71,6 +71,6 @@ import net.minecraft.world.entity.Entity;
     private boolean addEntityUuid(final T entity) {
         org.spigotmc.AsyncCatcher.catchOp("Entity add by UUID"); // Paper
-        if (!this.knownUuids.add(entity.getUUID())) {
+        if (this.knownUuids.put(entity.getUUID(), entity) != null) {
             LOGGER.warn("UUID of added entity already exists: {}", entity);
             return false;
         } else {
@@ -338,7 +338,7 @@ import net.minecraft.world.entity.Entity;
     }
 
     public boolean isLoaded(final UUID uuid) {
-        return this.knownUuids.contains(uuid);
+        return this.knownUuids.containsKey(uuid);
     }
 
     public LevelEntityGetter<T> getEntityGetter() {
